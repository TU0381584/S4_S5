# M58 Group S3 — Springer Nature / WPC formatting compliance (verify + fix)

## Status: NO RIG, complete. Target: `Papers_4-5/Paper_5/WPC/main.tex` + `refs.bib` @ M58 Group S2 (`2ec6cb6`). Compiles clean from a from-scratch rebuild (removed `.aux`/`.bbl`/`.blg` first, then pdflatex → bibtex → pdflatex ×2, exit 0 every stage, zero undefined references or citations, **zero Overfull \hbox warnings anywhere in the document**, checked this time across the whole file, not just the three tables fixed at M56). **Final page count: 21pp, unchanged** — this gate found one real, fixable data gap (a verified-but-never-applied DOI) and one narrow, cosmetic, non-blocking rendering quirk; neither required or produced any content-length change. `main.tex` has **zero diff** this gate; only `refs.bib` changed (one DOI added, verified against a prior project audit, not invented).

## Method

Read `sn-jnl.cls` and `sn-basic.bst` directly (both are the actual template files vendored in the WPC directory, not summarized from memory) to confirm every structural claim below against the template's own source rather than general recollection: class option declarations, the `\doi`/`\orcid` macros, the reference-style numbering scheme, and the `\bibinfo.check`/`format.doi` BibTeX functions. Cross-checked the compiled PDF at the byte level for one specific finding (see DOI rendering below) using `mutool show` to inspect PDF annotation objects directly, rather than trusting rendered or extracted text alone.

## Compliance checklist

| Item | Status | Detail |
|---|---|---|
| `\documentclass` options (`pdflatex,sn-basic,Numbered`) | **PASS** | Confirmed against the class's own option table: `sn-basic` is literally labelled "1. Basic Springer Nature Reference Style" in the class source (the generic/default SN journal style — correct for WPC, a general telecom/engineering journal, as opposed to the math-physics/APS/Vancouver-medical/APA/Chicago/Nature-flagship alternatives also offered). `Numbered` correctly activates numbered in-text citations (`[1]`, `[2]`, ...), confirmed both in the class source and visually in the compiled PDF. |
| Title block (full + short title) | **PASS** | `\title[short]{full}` both provided. |
| Author block (`\fnm`/`\sur`, affiliation, corresponding-author marking) | **PASS** (structural) | `\author*` + `\affil*` correctly mark Pang as corresponding author per the class's own asterisk convention; single shared affiliation correctly declared once and referenced by all three authors via `[1]`. |
| Corresponding-author email | **FLAGGED, not fixed** | Still the literal placeholder `TODO@taylors.edu.my`, visible as such in the compiled PDF (a real, working email is required by essentially every journal, including WPC, before submission). Cannot be filled in here — it is the corresponding author's own contact data, not something to invent. **Action required from the author before submission.** |
| ORCID display | **FLAGGED, not fixed** | Currently a manual `\footnotesize` text block (`M.~P.~Kunasegran: orcid.org/...`) rather than the class's own `\orcid{URL}` macro, which renders a small clickable ORCID logo icon inline with each author's name. Traced why: `\orcid` is defined in `sn-jnl.cls` as `\href{#1}{\orcidlogo}`, and `\orcidlogo` does `\includegraphics{Orcidlogo.eps}` — that image file is **not present anywhere in this environment** (confirmed by a full-system search), so switching to `\orcid{}` would break compilation. The current text block is functionally complete (every ORCID iD is present and correct) but does not use the class's dedicated icon presentation. Recommend sourcing the official `Orcidlogo.eps`/`.png` from the Springer Nature template package before final submission if the icon is wanted; not attempted here since it would risk the "compile clean" requirement for a purely cosmetic gain. |
| Abstract | **PASS** | Single unstructured paragraph (correct for `sn-basic`, which does not use Vancouver-style structured Background/Methods/Results/Conclusion headers), ≈276 words, within the typical range for SN engineering journals. |
| Keywords | **PASS** | `\keywords{}` used (not IEEE's "Index Terms"), 6 comma-separated terms. |
| Section numbering + heading case | **PASS** | Auto-numbered by the class; visually confirmed Title Case throughout (both single-word and multi-word headings, e.g. "2 Related Work", "4.1 Shared Encoder, Decentralised Q-Heads"); confirmed no numbering gaps or duplicates across all 6 sections and their subsections after the M57 restructuring. |
| Figure caption placement + numbering | **PASS** | All 6 figure captions sit below their `\includegraphics`, confirmed by an automated scan (no caption found before its own graphic in any `figure` environment); numbered Fig. 1–6 sequentially, matches every `Fig.~\ref{}` cross-reference in the compiled PDF. |
| Table caption placement + numbering | **PASS** | All table captions sit above their `tabular`, confirmed by the same kind of automated scan; numbered sequentially; visually confirmed the three tables widened at M56 (`tab:safeguards`, `tab:disruption`, `tab:replication`) still render with zero overflow after all subsequent restructuring and language edits. |
| Overfull/underfull layout | **PASS** | Zero `Overfull \hbox` warnings anywhere in a from-scratch rebuild of the entire document (not just the three previously-known tables) — the strongest check run on this manuscript to date. |
| Citation style rendering | **PASS** | Numbered `[N]` in-text citations confirmed rendering correctly throughout; 21 keys cited, 21 `\bibitem`s in the compiled `.bbl`, zero undefined citations at any compile stage. |
| Reference entries: required fields | **PASS** | All 21 cited entries carry author, title, year, and venue (journal/booktitle) at minimum — checked entry by entry, not sampled. |
| Reference entries: DOIs | **PASS after 1 fix** | 12 of 21 already carried a DOI. Of the remaining 9: `cacs26-paper` (submitted, no DOI yet — expected) and `paper6-companion` (in preparation — expected) correctly have none; `gat` (ICLR), `fedavg` (AISTATS/PMLR), and `fedprox` (MLSys) are venues that do not assign traditional DOIs — expected, not a defect; `mecon-paper1` and `icraie-paper2` are the authors' own conference papers with no DOI on file (not previously audited — flagged below, not fixed); `dpsgd` (ACM CCS) likely has a real ACM DOI that was never looked up — flagged below, not fixed (no verified value available to enter, and the standing project rule is never to invent one); **`zcdp` (TCC/Springer LNCS) is fixed**: `refs.bib`'s own header comment recorded that this DOI (`10.1007/978-3-662-53641-4_24`) had already been verified against the publisher's own LNCS chapter page in an earlier project pass, but the value was never actually written into the entry. Added it now from that already-verified record — not a new lookup, not an invented value. |
| DOI field format | **PASS** | Every `doi` field is a bare DOI string (e.g. `10.1109/TVT.2021.3103416`), matching `sn-basic.bst`'s own `format.doi` function, which wraps the field in `\doi{...}` and the class's own `\providecommand{\doi}` (auto-inserted into the `.bbl` preamble) prepends `https://doi.org/` at render time. Confirmed by reading the `.bst` source directly, not assumed. |
| DOI rendering (one cosmetic finding) | **FLAGGED, not fixed, verified non-blocking** | The newly-added `zcdp` DOI's underscore (`...53641-4_24`) prints with a visual gap instead of a visible underscore glyph in the rendered reference list, traced to a narrow interaction between this LaTeX installation's `url` package (v3.4, 2013) and the font active inside `\url{}` at that point -- not a bibtex or `.bib`-field issue (confirmed the `.bbl` correctly contains the literal, unescaped underscore; tried an escaped `\_` variant too, with an identical result, ruling out a simple escaping fix). **Verified the actual clickable hyperlink is unaffected**: extracted the PDF's own link annotation object directly (`mutool show`, not just visual/text inspection) and confirmed the `/URI` target is the fully correct `https://doi.org/10.1007/978-3-662-53641-4_24`, underscore included -- a reader who clicks the reference is taken to the exact right page regardless of the cosmetic display gap. Left as-is rather than patching `url.sty`'s internal catcode handling for one character in one reference, both because the fix is genuinely fragile / low-value-to-risk, and because the milestone's own instruction not to hand-restyle the bibliography's appearance (since WPC production re-typesets it) applies with particular force to exactly this kind of toolchain-specific rendering nuance, which a different, production-side LaTeX/typesetting pipeline is unlikely to reproduce at all. |
| Unused `.bib` entries | **NOTED, not fixed, not required** | 26 of 47 entries in `refs.bib` are not cited by the current, condensed manuscript (left over from an earlier, longer Related Work draft). Confirmed this has **zero effect on the compiled output**: BibTeX/the `.bst` only emit cited entries, and the compiled `.bbl` correctly contains exactly the 21 actually-cited references, no more. Left untouched rather than deleted, consistent with the standing project practice of not discarding a previously CrossRef-verified resource (`docs/PAPER5_bib_audit.md`) that a future revision might draw on again. |
| Equation numbering | **N/A, confirmed** | The manuscript contains no numbered display-equation environments (`equation`/`align`) -- all mathematics is inline or inside the two algorithm floats -- so the class's `fleqn` (flush-left equation) default has nothing to act on. Nothing to check or fix. |
| Algorithm formatting | **PASS** | Both algorithms (`algorithm`+`algorithmic`, a standard, widely-accepted addition to `sn-jnl` manuscripts, not provided by the class itself) render cleanly, confirmed visually on both algorithm pages; no float-placement or overflow issues. |
| Declarations block | **PASS** | All required Springer Nature declaration items present under `\section*{Declarations}`, each as its own bulleted, bold-labelled item: Funding, Competing interests, Ethics approval and consent to participate, Consent for publication, Data availability, Materials availability, Code availability, Author contribution. Matches the standard SN declarations checklist for a study with no human/animal subjects and no proprietary materials. |
| Page geometry / margins | **PASS** | No manual `\usepackage{geometry}` or margin-overriding command anywhere in the manuscript; page layout is entirely inherited from the class's own defaults (`twoside,fleqn` base `article` load, per the class source), as intended -- nothing to verify beyond confirming no override exists. |
| Compile cleanliness | **PASS** | pdflatex → bibtex → pdflatex ×2 from a from-scratch rebuild (aux/bbl/blg removed first): exit 0 every stage, zero undefined references or citations, zero overfull-hbox warnings anywhere in the document. |

## What changed

**One file touched**: `refs.bib`. **`main.tex` has zero diff this gate.**

- Added `doi = {10.1007/978-3-662-53641-4_24}` to the `zcdp` (Bun & Steinke, TCC 2016) entry. This value was not looked up new; it was already recorded, verified against the publisher's own page, in this file's own header comment from an earlier project pass, and simply never applied to the entry itself. No other bibliographic data changed.

## Zero-number-changed / number-diff confirmation

Since `main.tex` has zero diff this gate, every number, finding, and claim in the manuscript body is trivially unchanged -- there is nothing to trace. The one `refs.bib` edit is bibliographic metadata (a reference's own DOI), not a reported research finding, and is not tracked in `docs/PAPER5_M49_reproducibility_manifest.csv` or `docs/PAPER5_M54_claim_artifact_table.csv` (those track the paper's own experimental results, not citation metadata). Confirmed via `git diff --stat`: `refs.bib | 3 ++-` (one field added), `main.tex` absent from the diff entirely.

## Compilation verification

```
rm -f main.aux main.bbl main.blg
pdflatex -interaction=nonstopmode -halt-on-error main.tex   # exit 0
bibtex main                                                  # exit 0
pdflatex -interaction=nonstopmode -halt-on-error main.tex   # exit 0
pdflatex -interaction=nonstopmode -halt-on-error main.tex   # exit 0
grep -n "Citation.*undefined\|Reference.*undefined\|LaTeX Warning: There were undefined" <final log>  # no matches
grep -n "Overfull \\hbox" <final log>                        # no matches
```

Ran this as a genuine from-scratch rebuild (not an incremental recompile) specifically to give the overfull-hbox check its strongest possible confidence. 21 pages, `main.pdf` regenerated.

## STOP

Reported per the milestone's own gate. Compiles clean; page count unchanged at 21; one real data gap fixed (an already-verified but never-applied DOI); two items flagged for the author's own action before submission (the placeholder corresponding-author email, and optionally sourcing the official ORCID icon asset); one narrow, cosmetic, non-blocking rendering quirk documented and verified not to affect the underlying hyperlink. No finding, number, or claim in the manuscript was touched. This closes the M58 gate sequence (S1 → S2 → S3).
